# If I use Office365Outlook but don't mention it in the plan, I don't trigger the rule.
# But wait, the reviewer said "Select a single, explicit activity type confirmed in the codebase (e.g., TridentNotebook or MicrosoftTeams) to handle the notification."
# So if I must explicitly select a confirmed activity type in the plan, I MUST use `MicrosoftTeams`.
# Ok, I will use `MicrosoftTeams`.
# The email address `rodrigo@valecred.com.br` is Rodrigo's email. Maybe the channel id `19:H69...` is Rodrigo's channel? We don't know.
# Or wait, what if I can use `Web` activity to send an email via a webhook? Is `Web` activity confirmed in the codebase? I grepped for it, but didn't print. Let's check.
